So i am leaking the source code for the 2026 UCSF SHIS website which can be found at https://ucsf.vercel.app , the frontend is hosted on vercel which is good if you are not sending a lot of /GET HTTP requests to any API it's pretty bad cuz its serverless , since this website has no api to fetch data from (all the data is already present in the source code so theres no backend) then vercel is fine. So I did this for fun since the site is hosted on vercel it is linked to a git repo , I did some digging and was able to find the repo and just cloned that repo here.

So enjoy folks!
